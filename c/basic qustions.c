#include <stdio.h>
int main()
{
    // earthmatic opratore

    // float x, y, r;
    // printf("\nenter the value of x=");
    // scanf("%f", &x);
    // printf("\nenter the value of y=");
    // scanf("%f", &y);
    // printf("\n");
    // r = x + y;
    // printf("\n+ result= %f", r);
    // r = x - y;
    // printf("\n- result= %f", r);
    // r = x * y;
    // printf("\n* result= %f", r);
    // r = x / y;
    // printf("\n/ result= %f", r);
    // r = x % y;
    // printf("\n&result= %f", r);
    // // flote mai reminder nhi niklta
    //========================================================================================
    //in given number how much not of 2000, 500, 200, 100, 50, 20, 10, 5, 2, 1

    // int num,
    //     not, rem;
    // printf("enter the num.", num);
    // scanf("%d", &num);
    // not = num / 2000;
    // rem = num % 2000;
    // printf("\nnot of 2000rs =%d", not );
    // not = rem / 500; // 4567
    // rem = rem % 500;
    // printf("\nnot of 500rs =%d", not );
    // not = rem / 200;
    // rem = rem % 200;
    // printf("\nnot of 200rs =%d", not );
    // not = rem / 100;
    // rem = rem % 100;
    // printf("\nnot of 100rs =%d", not );
    // not = rem / 50;
    // rem = rem % 50;
    // printf("\nnot of 50rs =%d", not );
    // not = rem / 20;
    // rem + rem % 20;
    // printf("\nnot of 20rs =%d", not );
    // not = rem / 10;
    // rem = rem % 10;
    // printf("\nnot of 10rs =%d", not );
    // not = rem / 5;
    // rem = rem % 5;
    // printf("\nnot of 5rs =%d", not );
    // not = rem / 2;
    // rem = rem % 2;
    // printf("\nnot of 2rs =%d", not );
    // rem = rem % 1;
    // printf("\nnot of 1rs =%d", not );
    //========================================================================================
    // reminder and result

    // int x, y, res, rem;
    // printf("enter the value  of x=");
    // scanf("%d", &x);
    // printf("\nenter the value of y=");
    // scanf("%d", &y);
    // res = x / y;
    // printf("result=%d", res);
    // rem = x - res * y;
    // printf("\nreminder=%d", rem);
    //========================================================================================
    // percentes and total of 5 subbjects
    float x;
    int y;
    float res;
    printf("enter a desimal value x=");
    scanf("%f", &x);
    printf("enter a int value y=");
    scanf("%d", &y);
    res = x - y;
    printf("result=%f", res);
    float h, e, m, s, c, p, total, per;
    printf("hindi = ");
    scanf("%f", &h);
    printf("engish = ");
    scanf("%f", &e);
    printf("maths = ");
    scanf("%f", &m);
    printf("science = ");
    scanf("%f", &s);
    printf("physics = ");
    scanf("%f", &p);
    // total karna hai ab
    total = h + e + m + s + p;
    printf("total = %f", total);
    per = (h + e + m + s + p * 100) / 5;
    printf("\nper = %f", per);
    //========================================================================================
    //pre increament post increament
    // int a, b;
    // printf("enter the value of a = ");
    // scanf("%d", &a);
    // printf("\n new value of a = %d", ++a);
    // printf("\n new value of a = %d", a);
    // printf("\nenter the value of b = ");
    // scanf("%d", &b);
    // printf("\nchange value of b = %d", --b);
    // printf("\nnnew value of b = %d", b);
    //========================================================================================
    // if or else
    // find gross salary by given basic salary

//     int gs, bs; // gs= gross salary bs=basic salary
//     printf("enter basic salary = ");
//     scanf("%d", &bs);
//     if (bs <= 10000)
//     {
//         bs = bs + (bs * 20 / 100) + (bs * 80 / 100);
//         printf("your gross salary = %d", bs);
//     }
//     else if (bs <= 20000)
//     {
//         bs = bs + (bs * 25 / 100) + (bs * 90 / 100);
//         printf("your gross salary = %d", bs);
//     }
//     else if (bs > 20000)
//     {
//         bs = bs + (bs * 30 / 100) + (bs * 95 / 100);
//         printf("your gross salary = %d", bs);
//     }

//     return 0;
// }
//========
